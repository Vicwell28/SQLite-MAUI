<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SQLite_MAUI.Views.TodoItemPage"
             Title="TodoItemPage"
             BackgroundColor="Red">
    <VerticalStackLayout BackgroundColor="Red">
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <!--<Button Text="GetItemsAsyncCommand" Command="{Binding GetItemsAsyncCommand}"></Button>
        <Button Text="SaveItemAsyncCommand" Command="{Binding SaveItemAsyncCommand}"></Button>
        <Button Text="DeleteItemAsyncCommand" Command="{Binding DeleteItemAsyncCommand}"></Button>
        <Button Text="UpdateItemAsyncCommand" Command="{Binding UpdateItemAsyncCommand}"></Button>-->
    </VerticalStackLayout>
</ContentPage>